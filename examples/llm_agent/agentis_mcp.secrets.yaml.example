agents:
  llm_agent:
    llm:
      # OpenAI configuration
      # provider: "openai"
      # api_key: "sk-your-openai-api-key"
      
      # Anthropic configuration
      # provider: "anthropic"
      # api_key: "sk-ant-your-anthropic-api-key"
      
      # For testing use the mock provider (doesn't require API key)
      provider: "mock"
      api_key: ""